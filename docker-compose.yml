services:
  admin:
    image: ghcr.io/copycord/copycord:v2.0.0
    environment:
      - ROLE=admin
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data

  server:
    image: ghcr.io/copycord/copycord:v2.0.0
    environment:
      - ROLE=server
    volumes:
      - ./data:/data
    depends_on:
      - admin

  client:
    image: ghcr.io/copycord/copycord:v2.0.0
    environment:
      - ROLE=client
    volumes:
      - ./data:/data
    depends_on:
      - admin
